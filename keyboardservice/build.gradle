apply plugin: 'com.android.application'

android {
    namespace 'com.sys.tool.inputpad'
    compileSdk 33

    defaultConfig {
        applicationId 'com.sys.tool.inputpad'
        minSdk 21
        targetSdk 33
        versionCode 2
        versionName "2.1"
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
}
